[project]
name = "frappe-attachments-s3"
version = "0.0.1"
requires-python = ">=3.6,<3.10"

dependencies = [
    "xlsxwriter",
    "gspread",
    "geopy",
    "qrcode"
]

[tool.bench.frappe-dependencies]
frappe = ">=13.0.0,<14.0.0"
